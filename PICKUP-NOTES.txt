
2024 - 01 - 08

went with controller_weaken / hack / grow

Can grow hack and weaken with simple lite scripts

Reorganizing global data into server_actions to feedback on the server_status list and the 
server list it self. Should i continue to buffer the shit? or leave the expenses to controller_*.ts ? 

I think controller_* is better, cause then i can split the controllers to different script hosts, if 
i need to. Configuring controllers to script_hosts might be annoying...


Dec=26-2024

Started hack/controller.ts

Needs to select weaken threads properly
needs to implement grow and hack

Dec-24-2024

Implemented new hackable server. Problems include memory bloat, too many functions on >100 thread scripts (auto / hackableserver.ts)

Need to split weaken and hack/grow into two separate scripts, so that we divide up some of the memory bloat, and also
allow weaken to be more intelligent. Right now its downright stupid.


Dec-20 2024
right now seems to be focusing on mirroring /dist.

need to get that pointed at a src, and no longer transpile or compile
the ts to js, since bitburner can natively do ts now.

